import { AccessPolicy, AuthenticableEntity, EntityManifest } from '@repo/types';
export declare const policies: Record<AccessPolicy, (user: AuthenticableEntity, entity: EntityManifest, options?: {
    allow?: string[];
}) => boolean>;
