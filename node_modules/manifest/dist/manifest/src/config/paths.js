"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const path_1 = __importDefault(require("path"));
exports.default = () => {
    return {
        paths: {
            adminPanelFolder: process.env.NODE_ENV === 'contribution'
                ? path_1.default.join(process.cwd(), '..', 'admin', 'dist')
                : `${process.cwd()}/node_modules/manifest/dist/admin`,
            publicFolder: process.env.PUBLIC_FOLDER || `${process.cwd()}/public`,
            manifestFile: process.env.MANIFEST_FILE_PATH ||
                `${process.cwd()}/manifest/backend.yml`,
            handlersFolder: process.env.MANIFEST_HANDLERS_FOLDER ||
                path_1.default.join(process.cwd(), 'manifest', 'handlers')
        }
    };
};
