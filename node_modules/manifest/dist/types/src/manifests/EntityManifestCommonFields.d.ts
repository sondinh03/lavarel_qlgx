import { EntityManifest } from './EntityManifest';
export interface EntityManifestCommonFields extends Pick<EntityManifest, 'className' | 'nameSingular' | 'slug' | 'single' | 'properties' | 'hooks' | 'middlewares'> {
}
